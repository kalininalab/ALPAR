
process {
    conda.enabled = true
    
    // Default environment for most processes
    conda = "${projectDir}/environment.yml"

    withName: 'RUN_SNIPPY' {
        conda = 'bioconda::snippy=4.6.0'
    }

    withName: 'RUN_BAKTA' {
        conda = 'bioconda::bakta=1.9.3'
    }
    
    withName: 'RUN_PANAROO' {
        conda = 'bioconda::panaroo=1.3.3'
    }
    
    withName: 'RUN_PYSEER' {
        conda = 'bioconda::pyseer=1.3.11'
    }
}

profiles {
    standard {
        process.executor = 'local'
    }
    slurm {
        process.executor = 'slurm'
    }
}
